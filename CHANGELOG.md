# 更新日志

本文件按发布版本记录面向用户的重要改动。开发过程中的临时诊断、仅测试调整和构建缓存不在此记录。

## 未发布

### 修复

- 修复 Windows 下程序所在路径包含中文时，识别模板读取失败、点击开始后报 `Cannot load template` 并停止的问题。模板加载改为读取文件字节后解码，保留原有颜色通道和透明遮罩校验。

### 改进

- 源码与发布版统一记录详细日志，不再区分极简和详细模式。默认保留 7 天内最近 20 次运行，每个日志文件约 10 MB、最多 3 个历史分片，目录总容量目标为 500 MB；在开始、轮转和结束时按整次运行清理历史日志，并用文件锁保护活动运行。新分片重复运行环境和窗口摘要，清理失败记录警告。

## v1.2.2 — 2026-09-10

### 修复

- 同步 WGC 自适应滚动稳定检测的文档描述，并修正非参考分辨率下已过期的客户区点击断言。

### 改进

- 按职责拆分界面与自动化模块，统一停止控制、状态发布和滚动校验的边界，保持现有操作流程与识别规则。
- 整理校准、实机验证和发布工具目录，统一工具入口与资源路径；模板提取改为显式指定来源图片，网络模板工具仅在命令执行时处理文件。
- 完善开发指南与用户行为说明，保留项目根目录的规划记录并排除误提交。
- 将 Qt 与 PyWinRT/WGC 回归测试隔离到独立 Python 进程，并加强源码、开发环境、独立 EXE 和发布文档的版本一致性校验。
- 将窗口消息输入风险与免责声明纳入随发布包分发的中文使用说明。

### 发布

- Python 包、应用说明和本地 Windows x64 构建包版本统一更新为 `v1.2.2`。
- 目标构建包名称更新为 `E7auto_v1.2.2_x64.zip`。

## v1.2.1 — 2026-09-09

### 修复

- 修复 `1536 × 864` 等非参考客户区下，五位及更长天空石余额被固定数字 ROI 裁掉末位、导致刷新前识别超时的问题。
- 修复余额位数增加并推动天空石图标左移后，图标可能超出原有窄搜索区域的问题。

### 改进

- 天空石图标改为只在完整顶部栏搜索带内定位；余额改为从图标右侧动态读取第一组连续数字，并在远处铃铛等控件前停止，不再预设数字位数上限。
- 保留数字置信度、类别领先度、连续三帧稳定和刷新后严格 `before - 3` 校验；新增参考、2K 和 4K 派生客户区回归覆盖。

### 发布

- Python 包、应用说明和本地 Windows x64 构建包版本统一更新为 `v1.2.1`。
- 目标构建包名称更新为 `E7auto_v1.2.1_x64.zip`。

## v1.2.0 — 2026-09-07

### 新增

- 新增基于 Windows Graphics Capture 的游戏窗口捕获，游戏被其他窗口完全覆盖时仍可识别画面。
- 新增基于 Win32 窗口消息的后台点击与滚轮输入，不再占用或移动用户的真实鼠标。
- 新增后台启动支持；游戏处于后台时不再抢占前台，启动前已最小化时以无激活方式恢复。
- 正常完成刷新预算或无目标策略后，先退出秘密商店并确认返回主界面，再发布最终停止状态。
- 悬浮窗新增停止后关闭、随时拖动、绿色收起按钮和圆形 Logo 收起形态；单击圆形图标恢复，每次运行先显示完整悬浮窗。

### 改进

- 滚动后使用新帧、连续稳定和完整位移三重门控，在 `800 ms` 上限内自适应确认底部画面。
- 圆形悬浮 Logo 改用 `256 × 256` 资源并按设备像素比例缓存绘制，适配当前 200% DPI 及其他常见缩放比例。
- 圣约书签和神秘奖牌统计使用高亮绿色；悬浮窗位置在拖动后持久保存。

### 移除

- 移除真实鼠标控制、MSS/PrintWindow 捕获、F6 移动模式以及悬浮窗截图排除路径，不保留运行时回退。

### 发布

- Python 包、应用说明和目标 Windows x64 发布包版本统一更新为 `v1.2.0`。
- 目标发布包名称更新为 `E7auto_v1.2.0_x64.zip`。

## v1.1.5 — 2026-09-05

### 新增

- 新增模块化功能中心，当前提供 `刷新秘密商店` 功能入口，并为后续功能模块保留三个独立占位卡片。
- 新增统一的 E7auto 应用图标资源，用于窗口标题栏、Windows 任务栏、EXE 文件、开始菜单和快捷方式。

### 改进

- 重构主窗口为可自由缩放、最大化和还原的响应式界面；较窄窗口下功能卡片自动切换为单列滚动布局。
- 重新设计 `刷新秘密商店` 功能页：天空石上限改为无加减按钮的纯数字输入框，友情点数开关使用精确点击区域，启动按钮改为绿色。
- 商店卡片背景图按比例覆盖并聚焦裁剪，在窗口尺寸变化时保持图像比例。

### 发布

- Python 包版本、应用版本说明和 Windows x64 发布包统一更新为 `v1.1.5`。
- Windows x64 发布包更新为 `E7auto_v1.1.5_x64.zip`，并包含完整 UI 图像资源与多尺寸 Windows 图标。

## v1.1.4 — 2026-09-04

### 改进

- 简化模板生成、校准和发布验证流程，改为依赖文件可读性、图片结构、裁剪几何、蒙版像素、识别置信度及行为回归。
- 更新模板清单与维护脚本，使游戏 UI 变化后可以直接使用新素材重新校准，无需同步维护旧文件身份数据。

### 发布

- Python 包版本、应用版本说明和 Windows x64 发布包统一更新为 `v1.1.4`。
- Windows x64 发布包更新为 `E7auto_v1.1.4_x64.zip`。

## v1.1.3 — 2026-08-30

### 新增

- 增加不同桌面分辨率适配：`3120 × 2080` 继续使用 `2322 × 1306` 原始客户区，其他受支持分辨率按显示器完整宽度的 60% 调整游戏窗口，并保持校准宽高比。
- 增加显示环境安全校验：当前桌面分辨率低于 `2560 × 1440` 时拒绝运行；运行中更换显示器、修改分辨率或 DPI 时，在下一次输入前停止。
- 增加商店入口双状态确认：入口未出现时，仅在连续确认仍停留主界面后进行有限次数重试。
- 增加非参考分辨率下的 ROI 按需归一化和坐标映射，并记录详细模式下的归一化性能统计。

### 改进

- 改进天空石余额 OCR：统一数字字形尺寸，缓存正常、轻微加粗和轻微变细模板变体，并同时要求绝对质量与数字类别领先度；当前类别领先度为 `0.08`。
- F6 暂停期间不再消耗识别和等待超时时间。
- 非参考分辨率下的识别锚点、点击点、滚轮位置和悬浮窗安全区域统一映射自 `2322 × 1306` 校准空间。
- 保持原始分辨率零归一化路径；实测完成 33 次刷新并按预算正常结束。

### 修复

- 修复不同分辨率下窗口尺寸、识别区域与输入坐标不一致导致的识别或点击失败。
- 修复商店入口点击未生效后直接等待至超时的问题。
- 修复低置信度数字或相近数字类别可能导致天空石余额无法稳定确认的问题。
- 修复极简日志保留重复输入失败坐标的问题，并使日志轮转文件正确参与保留期限和数量清理。

### 发布

- Windows x64 发布包更新为 `E7auto_v1.1.3_x64.zip`，并改为通过 GitHub Releases 分发。
- 发布版继续使用极简日志；OCR 轮询、置信度、坐标和性能明细仅在详细日志中记录。

## v1.0.0 — 2026-08-29

- 首个 Windows x64 发布版。
- 提供商店进入、上下层商品识别、购买、刷新预算控制、网络恢复、安全停止、悬浮窗及极简运行日志。
