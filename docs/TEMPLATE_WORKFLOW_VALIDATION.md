# 模板登记流程验证记录

## 2026-09-15 执行约束与变更检查补强

- 根目录原无项目级 `AGENTS.md`，本次新增，明确新增和修改已有模板都必须走候选验证、prepare、核对及apply。
- 新增 `scripts/templates/check_changes.py`，只读比较Git基准与工作区、暂存区或指定提交；检查PNG归属、清单哈希、登记来源及其哈希，防止直接覆盖PNG后刷新哈希或沿用旧来源记录。
- 新增 `.gitattributes` 保留登记来源的原始字节，避免Windows Git换行转换破坏来源哈希；按用户要求移除尚未发布的CI配置，保留本地检查命令，未安装本地hook。
- 首轮检查成功拒绝当前天空石未登记的修改。补登记后工作区检查通过，识别到1个变更模板。
- 新检查13项测试及CLI入口1项通过；既有登记/恢复26项通过。测试包含新模板、已有模板替换、直接更新哈希、旧来源复用、来源篡改/丢失/越界、暂存遗漏、工作区与暂存/提交不同、无效基准和清单删除。
- 第一轮暂存测试失败暴露autocrlf转换问题；补充精确字节规则后，启用autocrlf的暂存与提交测试通过。
- 正式暂存区当前无修改，其检查结果为0个模板变更；含真实变更的暂存/提交路径在临时Git测试仓库验证，没有暂存或提交本项目文件。
- 发布模板资源校验返回空问题列表；CI配置已移除，未推送或触发远端CI。未跑全量测试、未打包。

### 天空石补登记证据

- 候选：`artifacts/template-candidates/20260915-233617-f407c9a9/shop/sky_stone_icon.png`（同目录有 `.candidate.json`）。
- 计划：`artifacts/template-candidates/20260915-233617-f407c9a9/registration_plan.json`。
- 已完成事务：`4c23b0cb4459435b8511298c21b0ac1c`，日志保留于 `artifacts/template-registration/transactions/`。
- 正式来源：`docs/calibration/registrations/shop-sky_stone_icon-c78eaf04b03d-424d239c56ae.json`。
- 原始不透明模板备份：`artifacts/sky-stone-cutout/sky_stone_icon_original.png`，已逐字节核对与当时HEAD中的原模板相同。
- 候选保留62×75尺寸与全部RGB像素，只修改alpha；反馈原图离线识别分数0.971766915，阈值仍0.93，正确读取23711。此前14项天空石相关测试通过，补登记未改变已验证PNG字节，因此未重复运行。
- 此前直接覆盖没有事务记录；本次是从当前已修改状态开始的补登记，没有伪造此前历史。本次事务旧PNG也是透明版本，原始不透明版本由上述独立备份保存。

以下保留首次实现的验证记录。

2026-09-15：完成候选导出隔离、显式功能路径解析、统一登记/恢复命令及发布验证衔接。使用说明见 [模板登记流程](TEMPLATE_WORKFLOW.md)。

## 实现范围

- `scripts/common/paths.py`：模板路径要求明确feature，按清单查键；未知键不再落入商店目录。候选目录使用时间及唯一编号，禁止候选输出写入正式模板、校准记录和配置目录。
- `scripts/common/candidates.py`：候选PNG附带尺寸和SHA-256校验文件；正式图像字节不由该导出过程更新。
- 现有图片提取工具默认导出候选及来源。网络裁剪增加来源记录，文字转换导出新候选、不修改输入。
- `scripts/templates/register.py`：prepare生成只读核对后的计划；apply验证计划未过时，复制PNG原始字节并更新指定清单条目及来源记录；异常回滚，未完成事务可recover；锁阻止工具并发写入。
- `scripts/release/verify_release.py`：当前文件列表以运行清单为准，校验登记后的来源记录哈希，保留历史行为证据校验。

## 直接相关验证

共95项不同用例已有通过结果，未运行全量测试：

- 原有CLI入口、模板图像、资源布局、数字识别和历史校准相关检查第一轮68项通过。
- 新增登记/恢复模块最终26项通过。
- 资源布局新增1项登记与发布验证集成测试；其所在模块最终18项通过。
- 最近一次组合执行登记模块、资源布局和CLI入口共69项通过；CLI防止计划覆盖输入的追加断言单项复验通过。重复项不累计。

检查覆盖候选只写任务目录、正式目录写入拒绝、默认路径唯一、明确归属、未知键/逃逸路径/文件归属冲突拒绝；PNG/校验文件/来源记录检查；预览无正式写入；过期或篡改计划拒绝；保留无关条目和配置；普通写入失败、最终加载失败回滚；中断后的恢复和重复恢复；外部修改保护、并发写锁；中文及空格路径下的CLI prepare/apply/recover完整流程。

中断测试使用注入的KeyboardInterrupt覆盖已替换图片、尚未提交清单的时点，没有对真实进程强杀或进行断电测试。多文件更新不是单次文件系统原子操作，行为和恢复限制已在使用说明中说明。

## 正式资源检查

开发开始记录了模板目录、正式校准目录与配置目录的64个文件哈希。收尾比较只有模板README因补充使用说明变化；正式PNG、运行清单、校准记录和运行配置无变化。登记、回滚和中断恢复只在测试临时项目执行。

`git diff --check`通过。未实机操作、未正式登记当前模板、未打包、未提交或推送。
