# 启动隐藏界面唤醒验证

2026-09-15：商店与企鹅初始入口统一接入单次唤醒。首次截图未识别到本功能入口时，通过原有窗口/F5保护和坐标换算，点击配置的客户区中心；成功发送后给予新的10秒识别预算，继续双重匹配和稳定帧确认，随后沿用原入口点击流程。

已经能识别到图标时不唤醒；连续失败也只发送一次启动唤醒。图标始终未恢复时识别超时停止，输入失败不重试。每次会话创建独立引擎，唤醒状态不跨运行保留。正常重新进入、点击后的页面确认及正常返回不启用启动唤醒；既有180秒策略等待唤醒保持不变。

网络恢复时丢弃被打断的启动帧并重新截图，再决定是否唤醒；恢复耗时不占识别预算，已经发送过的启动唤醒不会因重连再次发送。日志用 `input action=wake_main_screen_startup` 区分启动唤醒和其他点击，`startup_icon_confirmed` 记录最终确认及 `wake_sent`。

## 直接相关验证

- 原有商店会话、企鹅流程、首次识别/重新进入超时模块共85项通过；旧“首次未识别不得有任何输入”断言更新为只允许一次中心唤醒。
- 新增 `tests/automation/test_startup_wake.py` 26项通过：可见图标不唤醒；未命中后立即一次点击；新帧连续确认；闪烁重置稳定计数但不重复唤醒；无效唤醒超时；F5在点击前/后停止；窗口变化及输入失败；非基准窗口中心坐标；重连前后新鲜帧、12秒恢复不占预算及不重发唤醒；两个功能完整流程继续并正常结束。
- 原有策略恢复及网络恢复模块9项通过。

共120项不同相关用例通过，未运行全量测试。测试使用模拟窗口、输入、画面和时钟，没有操作真实游戏，尚未进行本轮实机验收。`git diff --check`通过。未重新打包、提交或推送。
