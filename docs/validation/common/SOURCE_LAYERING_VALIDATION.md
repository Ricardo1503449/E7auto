# 源码分层验证记录

2026-09-16：商店与企鹅改为平级流程，组合共享RuntimeContext；识别器组合共享模板匹配和数字组件。运行会话接收流程工厂及停止策略，生产服务由bootstrap在工作线程中组装。配置、平台、模板读取、日志分别归入对应层；配置格式和资源目录保持原约定。

## 验证范围

- 754项不同的直接相关用例分批通过；不是一次全套测试。逐项清单在本地维护任务的related-test-nodeids.json。
- 覆盖商店购买/刷新/滚动、企鹅价格和兑换、启动与重连、F5阻断、缓存截图、资源隔离、真实离线识别及缩放样本、配置边界、UI、日志、窗口/输入/WGC，以及受导入迁移影响的校准、验证、发布和模板登记工具。
- 新增源码边界用例验证跨功能导入、共享层反向依赖、Win32越界、旧模块引用、未知功能、WGC提前导入、隔离进程导入和实际源码规则。布局集成测试验证暂存区违规不能被工作区修正掩盖，反向情况亦然；Git操作仅发生在测试临时仓库。
- 原405个测试函数全部保留，新增6个测试函数（含参数化边界用例）。测试中的内部探针改为实际组件位置，保留原行为断言。
- 工作区布局/源码依赖检查、模板变更检查、Python语法和git diff --check通过。正式模板变更数为0。
- 模板、校准、配置和固定样本共74个文件与重构前SHA-256一致。

## UI进程异常及验证限制

连续运行28项UI用例时，在企鹅页面测试附近出现Qt原生进程异常，未形成Python断言失败。使用本轮开始保存的源码和测试基线，在独立目录复现同一测试位置的进程异常（退出码3221226505）。这不是仅凭未修改UI代码作出的推断。

源码分层验收当时将同一组用例拆成25项和3项两个独立进程后全部通过。WGC的24项测试另起进程，与Qt分离。该阶段没有修复Qt原生异常，也没有声称连续单进程UI测试稳定性通过；后续专项修复见下节。

未操作游戏、未执行实际截图/点击验证、未重新构建或验收新的发行包、未暂存提交或推送。窗口和WGC测试使用替代对象；源码回归不能替代真实环境和发行构建验收。

## 2026-09-16 后续UI生命周期修复

后续诊断中，同样的28项UI用例原样连续运行仍发生原生退出；临时禁用循环GC、在用例间主动回收、或把标题栏/缩放控件的主窗口回指改成弱引用，均能使该组通过。这些是受控诊断，不是提交关闭GC的修复。

专项修复将两个控件的反向强引用改为weakref，并在操作前同时检查引用存活与PySide包装对象的C++有效性。主窗口仍拥有子控件；生产窗口关闭语义不变。UI测试新增局部conftest，统一QApplication与本用例窗口清理，显式处理延迟销毁及计时器；通用测试helper不导入Qt辅助代码。

验证结果：

- 只修改生产代码、尚未加入清理fixture时，原28项UI测试在同一个进程通过，循环GC开启。
- 新增7项生命周期回归覆盖主窗口不被子控件保活、Python引用过期、C++对象已销毁、拖动/缩放派发、窗口与浮层/计时器实际销毁，以及两种关闭方式的原生压力回归。
- 使用本次修复前的真实window_chrome源码作为子进程对照，新增“主窗口必须释放”用例按预期失败，引用仍指向MainWindow；新实现通过。未以调整原断言掩盖问题。
- 最终 `tests/ui` 的38项用例在同一个pytest进程连续通过，退出码0。两个压力用例各在独立进程运行30轮创建、功能切换、浮层收起/恢复和关闭；仅close路径不显式调用gc.collect，显式销毁路径验证测试清理。两者均确认窗口包装对象及原生窗口无残留、循环GC开启、WGC/WinRT未加载。
- 布局/源码依赖、Python语法及差异检查通过；未运行全套或无关业务测试，未操作游戏或重新打包。该验证覆盖已复现的生命周期问题，不代替所有运行环境的长期稳定性验证；未取得Qt内部非法访问位置的原生调用栈。

专项证据目录：`artifacts/tasks/ui/20260916-170517-fix-native-lifecycle-crash-d4491581/`，原始基线在inputs，结果日志在results。

更早的Qt/WGC隔离后来在独立任务中定位为MSVC运行库版本冲突，并由运行库选择及构建一致性检查替代；见[平台依赖验证](../platform/BACKGROUND_VALIDATION.md#2026-09-16-qtpywinrt原生运行库共存)。上述分层及UI阶段的分进程结果保留为历史事实。

## 证据与维护

本地证据目录：`artifacts/maintenance/20260916-154205-source-layering-f9437fb7/`。包含源码基线、文件变化清单、模块迁移映射、受保护文件摘要、测试用例清单和UI当前/基线日志；一次性迁移脚本及复现副本位于scratch。

后续功能和修复继续遵循[开发指南](../../development/DEVELOPMENT.md)和[目录与产物约定](../../development/LAYOUT.md)。源码检查与产物分类共用现有交付入口，不重新分类docs/tests/artifacts/build。历史校准证据、归档和已完成的登记事务不重写。
